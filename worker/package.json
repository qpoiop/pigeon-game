{
  "name": "pigeonoid-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WebSocket relay (Durable Object) for PIGEON PROTOCOL online play.",
  "scripts": {
    "type-check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
